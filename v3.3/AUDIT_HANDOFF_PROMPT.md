# HANDOFF PROMPT FOR NEW SESSION — V3.3 PHASE 8 + FULL AUDIT

Copy everything below this line into a new Claude Code session:

---

Read `v3.3/SESSION_RESUME.md` and `.claude/plans/abundant-baking-cloud.md` FIRST.

## WHAT TO DO

### Part 1: Implement Phase 8 (18 items)

Phase 8 is the post-training fixes from the plan. Implement ALL of them BEFORE training so the live trader matches the optimizer's strategy. The plan has full specs with code snippets for each item.

**8A: Optimizer Fixes** (exhaustive_optimizer.py) — 5 items (8A.1-8A.5)
**8B: Live Trader Fixes** (live_trader.py + bitget_api.py) — 10 items (8B.1-8B.10)
**8C: Scale-In / Scale-Out** (live_trader.py + exhaustive_optimizer.py) — 3 items (8C.1-8C.3)

Use team agents — one per sub-phase (8A, 8B, 8C) working in parallel.

After implementation, run CASCADE DETECTION on every change:
1. Signature changes → grep all callers
2. Filename/path changes → grep all consumers
3. Config value changes → grep all readers
4. Interface contract changes → trace data flow

### Part 2: Full Audit (ALL phases 0-9 including Phase 8)

After Phase 8 is implemented, run the FULL audit on the plan at `.claude/plans/abundant-baking-cloud.md`:

**Audit Protocol:**
- Launch team agents (one per phase) with their own context windows
- Each agent reads the plan spec for their phase, reads the actual code, verifies line by line
- Audit → Fix → Repeat until clean
- After first clean pass, run **2 more clean passes** (3 total)
- If ANY fix is needed after the 3 clean passes, run **3 MORE clean passes** (total 6)
- Work phase by phase, micro-step by micro-step

**Phase-by-Phase Checklist:**
- Phase 9: Data downloads, DB completeness, row counts, date ranges
- Phase 0: Hardware detection (cgroup-aware CPU/RAM)
- Phase 1: Training-blocking (11 fixes)
- Phase 2: Training quality (13 fixes)
- Phase 3: Optuna (16 fixes)
- Phase 3B: LSTM (4 fixes)
- Phase 4: Cloud deploy (13 fixes)
- Phase 5: Untouched files (10 fixes)
- Phase 6: Documentation (25 fixes)
- Phase 7: Cascade verification (12 checks)
- **Phase 8: NEW — optimizer (5), live trader (10), scale-in/out (3)**

**Data Gap Audit:**
- BTC 15m: must have ~294K rows starting 2017-11-01
- BTC 4h: must have ~23K rows starting 2017-08-17
- BTC 1h: must have ~75K rows starting 2017-08-17
- Multi-asset crypto 1h/4h: all 13 assets must have full history to listing date
- All 16 DBs present + kp_history_gfz.txt

### Part 3: Deploy and Train

Once audit passes clean, deploy to cloud and train all 5 TFs.

## MATRIX THESIS (include in ALL agent contexts)

This is NOT a conventional trading bot. The edge is 2.9 MILLION sparse binary cross features generated by cross-multiplying esoteric signals (gematria, astrology, numerology, space weather, tweet decodes) with technical analysis indicators. LightGBM's EFB (Exclusive Feature Bundling) bundles mutually exclusive binary features — architecturally why LightGBM is used, never XGBoost. NaN = "missing signal" (LightGBM learns optimal split direction). 0 = "value is zero" (DIFFERENT meaning). NO feature can EVER be filtered, subsampled, or removed. The model decides via tree splits. feature_pre_filter=False is CRITICAL. Esoteric features ARE the edge.

## KEY FILES
- `.claude/plans/abundant-baking-cloud.md` — THE PLAN (authoritative)
- `v3.3/SESSION_RESUME.md` — Current state, audit history, data status
- `v3.3/CLAUDE.md` — Project rules, lessons learned, audit pipeline
- `v3.3/config.py` — All trading configs (TRADE_TYPE_PARAMS, DRAWDOWN_PROTOCOL, etc.)
- `v3.3/live_trader.py` — Live trading logic (Phase 8B target)
- `v3.3/exhaustive_optimizer.py` — Optimizer (Phase 8A target)
- `v3.3/bitget_api.py` — Exchange API (8B.7, 8B.8)
- `v3.3/inference_crosses.py` — Cross feature computer (8B.1)
